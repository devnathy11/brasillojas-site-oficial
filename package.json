{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "**"
  ],
  "scripts": {
    "build": "yarn typecheck && yarn workspaces run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "yarn typecheck:libs"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "stripe": "^22.0.2",
    "stripe-replit-sync": "^1.0.0"
  }
}
